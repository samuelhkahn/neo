; NEO training on UCSC Lux: LSST DP2 i-band (COSMOS) -> HST/ACS F814W (COSMOS-Web DR1).
; Paths use ${NEO_DATA}, expanded from the environment by train.py.
; Hyperparameters follow the published best configuration (see example.ini).

[DEFAULT]
hst_path_train = ${NEO_DATA}/pairs/cosmos_web_i/train/hr
hsc_path_train = ${NEO_DATA}/pairs/cosmos_web_i/train/lr
hst_path_val = ${NEO_DATA}/pairs/cosmos_web_i/val/hr
hsc_path_val = ${NEO_DATA}/pairs/cosmos_web_i/val/lr

[IDENTIFIER]
identifier = "lux_cosmosweb_i"

[COMET_PROJECT]
comet_project = neo-rubin-lsst

[COMET_TAG]
comet_tag = "lux LSST i -> HST F814W (COSMOS-Web)"

[BATCH_SIZE]
batch_size = 8

[GAN_STEPS]
gan_steps = 2000000

[HSC_DIM]
hsc_dim = 100

[HST_DIM]
hst_dim = 600

[DATA_AUG]
; Flips only (lossless). Kept off for the baseline, matching the published run; enable for the ablation.
data_aug = False

[DISPLAY_STEPS]
display_steps = 100

[LR]
lr = 0.0002

[LAMBDA_RECON]
lambda_recon = 200

[LAMBDA_SEGMAP]
lambda_segmap = 200

[LAMBDA_VGG]
lambda_vgg = 1

[LAMBDA_ADV]
lambda_adv = 1

[DISC_UPDATE_FREQ]
disc_update_freq = 1

[PRETRAINED_GENERATOR]
pretrained_generator =

[PRETRAINED_DISCRIMINATOR]
pretrained_discriminator =

[SAVE_STEPS]
; Numbered checkpoints (kept) every N steps.
save_steps = 25000

[VGG_LOSS_WEIGHTS]
vgg_loss_weights = [0.0, 1.0, 1.0, 0.0, 0.0]

[DISC_LR]
disc_lr = 0.00002

[CHECKPOINT]
ckpt_dir = ${NEO_DATA}/checkpoints/lux_cosmosweb_i
; latest.pt (overwritten) every N steps; this is what --resume reads.
latest_every = 2000

[DATALOADER]
num_workers = 8
